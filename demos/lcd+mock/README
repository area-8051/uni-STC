This project demonstrates the use of the LCD text menus API, as well 
as the use of a mock LCD controller to develop an application's user 
interface on PC rather than on the target MCU.

The executable should be run in a terminal window supporting ANSI/VT100
control sequences.
